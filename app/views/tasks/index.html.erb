<h1>Mission List</h1>

<% @tasks.each do |task| %>
<p><%= task.title %></p>
<%= link_to "Delete", task_path(task), method: :delete %>
<%= link_to "Edit", task_path(task), method: :patch %>
<% end %>
<%= link_to "Add a task", tasks_new_path %>
